'use strict';

class ServerBase {
  constructor() { }

  cwd (client, params) {
    this.cwd(client, params)
  }

  list (client, params) {
    this.list(client, params)
  }

  get (client, params) {
    this.get(client, params)
  }

  put (client, params) {
    this.put(client, params)
  }
}

module.exports = ServerBase;
