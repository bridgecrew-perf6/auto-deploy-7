'use strict';

class ECSBase {
  constructor() { }

  buildAvoidPwdTrust (params) {
    this.buildAvoidPwdTrust(params)
  }

  publish (params) {
    this.publish(params)
  }

  backup (params) {
    this.backup(params)
  }

  rollback (params) {
    this.rollback(params)
  }

}

module.exports = ECSBase;
